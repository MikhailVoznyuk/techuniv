function adaptiveLine() {
    const list = document.querySelector('.modal-list');
    const firstListUnit =  list.firstChild;
    const lastListUnit = list.lastChild;
    const line = document.createElement('div');
    line.classList.add('modal-list-line');
    line.style.top
}